defmodule Teebox.TokenTest do
  use Teebox.ModelCase
  import Teebox.Factory
  alias Teebox.Accounts

  test "create/2 returns :ok, token and new connection" do
  end

  test "create/2 returns :error, message and connection" do
  end
end
